{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/end_src/main.go", // 修改为你的入口文件
            "args": [],
            "env": {},
            "cwd": "${workspaceFolder}",
            "showLog": true
        }
    ]
}
